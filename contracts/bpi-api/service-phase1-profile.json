{
  "profile": "phase-1-java17-shadow-service",
  "description": "Public OpenAPI operations and trusted internal ingestion endpoints implemented by the real Java 17/PostgreSQL service.",
  "operationIds": [
    "getBpiOverview",
    "getCurrentLineState",
    "listBatchCandidates",
    "getBatchCandidate",
    "confirmBatchCandidate",
    "listBatches",
    "getBatch",
    "getBatchEvidence",
    "getBatchTimeline"
  ],
  "internalEndpoints": [
    {
      "method": "POST",
      "path": "/internal/bpi/v1/candidates",
      "purpose": "Authenticated candidate ingestion from the future stream adapter"
    },
    {
      "method": "POST",
      "path": "/internal/bpi/v1/telemetry",
      "purpose": "Feature-gated replay staging for telemetry persistence acceptance; disabled by default and not the production Kafka path"
    }
  ],
  "internalEndpointModes": {
    "/internal/bpi/v1/telemetry": "SHORT_LIVED_REPLAY_STAGING"
  },
  "mode": "SHADOW_ONLY",
  "excludedWrites": ["WOM", "QCS", "WMS", "PLC", "DCS"]
}
