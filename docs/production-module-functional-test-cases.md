# 生产模块功能测试用例矩阵

## 当前结论

本文件把生产模块从“页面能打开”提升为动作级验收清单。当前已经完成真实浏览器页面 smoke，并完成 WOM 制造任务“开始执行、保持、重启、无产出明细结束、完工报工产出明细、投入明细报工、提前放料确认、下推备料需求、工序开始/结束、活动开始/结束、简易活动报工、制造请检生成 QCS 请检单、QCS 审核生效后生成检验报告”的真实前端状态流转和 PostgreSQL 落库验收；请检和报告生成已不再是当前 blocker。QCS 报告编辑页原 `layoutJson` 500 已由 `119-qcs-inspect-report-edit-runtime-json.sql` 修复并在 PostgreSQL/API 层复验为 200，且已通过公网地址 `222.88.185.146:18080` 完成真实浏览器渲染复验；QCS 报告结果字段保存已通过真实页面运行时和 PostgreSQL 验收；报告审核提交到生效并回写 WOM 合格状态、不合格状态、自动生成不合格处理单以及处理单审核生效/WOM 处理单号回写已通过前端/API、LIMS/WOM 日志和 PostgreSQL 验收。本轮 marker `ADP_E2E_20260618_UNQLF_AUTO_DEAL_3` 验证 QCS 报告“不合格”生效后真实回写 WOM 任务、待入库记录、执行日志和批次状态，自动生成 `qcs_un_qlf_deals` 不合格处理单；后续 marker `ADP_E2E_20260618_UNQLF_DEAL_EFFECT` 验证处理单从审核节点生效，`qcs_un_qlf_deals.status=99`，WOM 任务/执行日志/批次均回填 `rejects_deal_id=756484120581376`。另外，QCS 产品紧急放行 edit/view 页原 `layoutJson` 500 已由 `124-qcs-inspect-release-runtime-json.sql` 修复，`125-qcs-inspect-release-action-compat.sql` 补齐 PostgreSQL 明细表、监督视图和工作流配置后，保存草稿 `ADP_E2E_20260618_INSP_RELEASE_SAVE_797449` 与提交审核 `ADP_E2E_20260618_INSP_RELEASE_SUBMIT_797449` 均完成前端/API/PostgreSQL 验收。2026-06-18 补测 WOM 领料退料、备料退料、批次退料和退料明细四个列表，公网入口真实浏览器均 `200` 且查询接口无错误；远端 PostgreSQL 确认 `wom_reject_materials`、`wom_rejct_matal_parts` 当前均为空，菜单元数据只暴露查询操作。同日补齐 WorkAppointment 恢复静态 HTML 的 `vendors.sesgis.js` 依赖后，作业计划编辑页和作业活动编辑页已真实渲染；随后 `148-runtime-visible-business-buttons.sql` 恢复 WorkAppointment 列表 `layoutJson` 的 `新增/删除` 按钮，`149-workappointment-i18n-resource-fixups.sql` 与 `148` 的 `namekey` 修正让列表显示中文 `新增/删除`，公网真实浏览器点击 `新增` 已打开 `workPlanEdit`，随后补充 `WAPSCommon` 初始化请求兼容并完成真实可见新增表单保存/删除清理，marker `ADP_E2E_20260619091757_WAPS_VISIBLE_FORM_SAVE` 已 PASS；调整/作废/审批已完成专项复验，组合用例改为 PASS。WTS 作业许可已通过真实浏览器页面上下文调用 `permitUltraSubmit` 保存主许可草稿并查 PostgreSQL 确认 `wts_work_permits` 落库，随后用删除接口清理为 `valid=false`；`148-runtime-visible-business-buttons.sql` 也恢复 WTS 列表中文 `新增/修改` 按钮，公网真实浏览器点击 `新增` 已打开 `workPermitEdit`。本轮 `152-wts-firework-workflow-config.sql` 补齐 `fireWorkWF` 的 PostgreSQL 工作流元数据后，marker `ADP_E2E_20260619095233_WTS_CHILD` 已证明动火子票 `wts_work_tickets`、开票申请待办 `wfm_task_pending` 和 `wts_work_tickets_di` 真实落库；随后 `153-wts-firework-runtime-json.sql`、`154-runtime-view-is-shadow-integer-compat.sql` 和 `patch-wts-runtime-compat.py` 修复动火节点页面/附件接口后，开票申请提交已生成风险确认与气体分析两个并行待办，两个后续页面真实浏览器渲染无错误。暂停/恢复已复验 PASS；选择 `WTS_processing/close` 后进入封票待办 PASS；本轮 marker `ADP_E2E_20260619142344_WTS_FIREWORK` 已完成从创建、审批、执行处理到封票生效的完整正常关闭，PostgreSQL 最终 `status=99/job_status=WTS_jobStatus/normalClose/pending=0`，正常关闭用例改为 PASS；未选处理方式已复验为业务校验 PASS，`WTS_processing/stop` 终止分支也已用 marker `ADP_E2E_20260619144549_WTS_FIREWORK_STOP` 完成真实前端/API/PostgreSQL 验收；若产品另有独立作废/cancel/delete 入口仍待确认。RM 批控配方导入入口已由运行时 HTML 补丁恢复，真实浏览器上传 Excel 后 `importMainXls` 返回导入成功，并在 PostgreSQL `rm_formulas` 查到 marker 行。当前 QCS/WOM 侧剩余 blocker 收窄为不良数登记和库存/入库相关回写；退料主链路已经通过备料退料、车间物料退料、批次退料三个专项真实前端/API/PostgreSQL 验收。让步放行处理方式已由 `ADP_E2E_20260618210018_QCS_DEGRADED_RELEASE` 专项 PASS，拒收 `return` 已由 `ADP_E2E_20260619_QCS_RETURN_1` 专项 PASS。`100.99.133.43:18080` 仍有 Tailscale 慢链路残余风险，本次 RM 导入和 WTS 后续节点浏览器证据使用同环境公网 HTTP 入口 `222.88.185.146:18080`，导出和跨模块主流程仍需继续逐项真实前端验收。

机器可读记录见 `metadata/production-module-test-cases.json`，结构校验命令为：

2026-06-19 新地址专项补测：SSH/DB/容器确认 `100.99.133.43` 与原 `10.11.100.17` 为同一台 `v6-2288H-V6` 测试机，浏览器大静态资源仍使用同环境公网入口 `222.88.185.146:18080` 更稳定。Marker `ADP_E2E_20260619_QCSM2` 真实前端触发 WOM `createManuInspect` 后直接调用 `/msService/WOM/quality/quality/syncInspectReport` `reportResult=true`，PostgreSQL 回查 WOM 任务/执行记录/待办为 `已检/合格`，批次 `BaseSet_checkResult/qualified`、`is_available=true`、`active_batch_state_id=11000`。Marker `ADP_E2E_20260619_QCSBAD` 同路径传 `reportResult=false`，接口返回 `needRejectReport=1`，PostgreSQL 回查 WOM 任务/执行记录/待办为 `已检/不合格`，批次 `BaseSet_checkResult/unqualified`、`is_available=false`、`active_batch_state_id=11003`。库存/入库回写仍不能标记 PASS：WOM/QCS 源码最终调用 `material` 服务的 `/material/foreign/foreign/checkProdResult` 和 `/public/material/produceOutSingle/produceOutSing/generateProduceOutSing`，但测试环境 Nacos `prod` 组没有 `material` 实例，认证后网关调用均返回 `503 can not find any tenant app service`；`BasicMs/BaseSet/LIMSMaterial` 替代路径为 `404`。最新本地包扫描已覆盖 `/Users/zhangchu/Documents/MES包` 和 ADP base-Server/config/static 根目录下 `58889` 个文件、`1142` 个外层 archive、`2139` 个第一方/依赖相关嵌套 archive 和 `429984` 个 archive entry，entry/text 上限跳过均为 `0`，只发现 `LIMSMaterial`、基础物料/仓库档案和 `SpareManage` 备件库存内容，未发现上述 `material/foreign` 或 `produceOutSingle` 被调用方实现；远端 PostgreSQL 只存在 `baseset_material*`、`baseset_warehouse*`、`wom_*material*` 等基础/WOM 内部表，不能替代独立仓储库存服务目标表。

2026-06-20 `100.99.133.43` material 服务依赖专项复验：远端容器清单没有 `material/wms/warehouse/stock/inventory` 服务；Nacos `prod` 组 `material/MATERIAL/wms/warehouse/inventory` 均为 `hosts=[]`，而 `WOMMs/QCS/RMMs/LIMS` 均有健康实例；登录 `admin` 后认证调用 `POST /msService/material/foreign/foreign/checkProdResult?srcId=1&checkResult=1` 与 `POST /msService/public/material/produceOutSingle/produceOutSing/generateProduceOutSing` 均返回 `503 can not find any tenant app service`，`BasicMs/BaseSet/LIMSMaterial` 替代路径返回 `404`。PostgreSQL 只扫描到 `baseset_material*`、`baseset_warehouse*`、`hm_*stock/material*` 和 `wom_*material*`，未验证到独立 material 服务目标表。专项报告见 `docs/backend-table-audit/material-service-dependency-analysis.md` 和 `metadata/material-service-dependency-analysis.json`。

追溯专项结论：WOM 制造任务列表“生产过程追溯”按钮入口已定位到 `ProcessAnalysis` 模块。真实浏览器使用 marker 批次 `ADP_E2E_20260618_UNQLF_AUTO_DEAL_3_BATCH` 请求 `/msService/ProcessAnalysis/analysisParam/analysisParam/isProdprocessView` 与 `/msService/ProcessAnalysis/processAnalysis/exelogSecond/processBatchViewOut` 均返回 `503`；2026-06-20 在 `100.99.133.43` 用仓库 Playwright request 登录 `admin` 后复验 `isProdprocessView`、`processBatchViewOut`、`analysisiTask`、`manualStatActive`、`manualStatProcess` 均返回 `503 can not find any tenant app service`，Nacos `prod@@ProcessAnalysis/processanalysis/PROCESSANALYSIS/Traceability/traceability` 均为 `hosts=[]`，而 `WOMMs` 有健康实例。远端 PostgreSQL 严格按 `^pa_`、`process.*analysis`、`trace`、`process_batch` 扫描为 0 张表，`runtime_view` 和 `rbac_menuinfo` 也无 `ProcessAnalysis/trace` 行；当前本地源码只找到 WOM 调用方脚本，未找到 `ProcessAnalysis` 被调实现或模块包。该项不是按钮缺失，而是当前测试包缺追溯模块包/服务/视图/表。专项报告见 `docs/backend-table-audit/processanalysis-dependency-analysis.md` 和 `metadata/processanalysis-dependency-analysis.json`。

2026-06-20 追加可复跑外部依赖 readiness smoke：`make smoke-business-dependencies` 会只读检查 Nacos `prod` 服务注册、PostgreSQL 元数据和认证依赖端点。当前 `metadata/business-dependency-readiness-smoke.json` 已在 `100.99.133.43` 刷新到 `2026-06-22T01:36:29.672Z`，结果仍显示 `material-service` 与 `process-analysis` 均为 `BLOCKED`：`material/MATERIAL/wms/warehouse/inventory` 均无健康实例，`material` 两个依赖端点返回 tenant-service `503`；`ProcessAnalysis/processanalysis/Traceability` 均无健康实例，5 个追溯端点返回 tenant-service `503`，且 PostgreSQL `processAnalysisTableCount/runtimeView/menu` 均为 0。该 smoke 用来在后续业务包部署后快速判断是否可以重新进入 marker 验收，不会写业务数据。

```bash
make production-testcase-check
```

## 已有真实前端证据

| 证据 | 结果 | 路径 |
| --- | --- | --- |
| 业务模块 API/layout smoke | `53/53` PASS | `/tmp/adp-business-module-smoke-20260615184508.json` |
| 业务模块真实浏览器页面 smoke | `53/53` PASS | `/tmp/adp-business-page-smoke-final-20260615204003/business-page-smoke-results.json` |
| WOM 制造任务动作发现 | 未发现新增入口；状态/备料/请检/追溯按钮已恢复 | `/tmp/adp-production-action-discovery-20260615180122/production-action-discovery.json` |
| WOM 生产动作候选页探测 | 2 个只读入口可达；5 个动作视图最初 `layoutJson` 500；1 个图形入口 404 | `/tmp/adp-production-action-discovery-candidates-20260615193213/production-action-discovery.json` |
| WOM runtime JSON 修复后复验 | 5 个动作视图真实浏览器渲染成功，console/network/request failure 均为 0；未提交写请求 | `/tmp/adp-production-action-discovery-20260616014350/production-action-discovery.json` |
| WOM 列表动作按钮复验 | `makeTaskList` 8 个动作按钮可见，`prepareMakeTaskList` 1 个备料按钮可见；两页均 `共 0 项`，未点击写动作 | `/tmp/adp-wom-button-visibility-20260615180122/wom-button-visibility.json` |
| WOM layoutJson 直连复验 | `makeTaskEdit/makeTaskSubmitView/makeTaskView/makeTaskBatchView/easyTaskOperateView` 均返回 `200`，pageType 为 `EDIT/VIEW`，含 `DataGridCode` 和真实 `produceTask` 字段 key | 命令输出已记录在本轮验收日志 |
| WOM PostgreSQL 兼容表回读 | `wom_wait_put_records` 存在、112 列、主键和 5 个索引存在；制造任务开始/保持/重启验收已写入或更新 `wom_produce_tasks`、`wom_wait_put_records`、`wom_proc_reports`、`wom_produce_task_exelog`；`080` 兼容触发器修复 hold/restart 待办状态同步 | 测试机 `10.11.100.17` PostgreSQL 查询；SQL：`deploy/docker/postgres/init/079-wom-wait-put-records-table.sql`、`deploy/docker/postgres/init/080-wom-wait-record-state-sync-trigger.sql` |
| WOM 制造任务开始执行落库验收 | 真实浏览器选中 marker `ADP_E2E_20260615192427_WOMSTART` 后点击“开始”，`updateTaskState` 返回 `dealSuccessFlag=true`，PostgreSQL 回查 4 张目标表均发生变化 | `/tmp/adp-wom-start-persistence-script-current.json`；可复验命令：`ADP_DB_SSH_PASSWORD=... make acceptance-wom-start-persistence` |
| WOM 制造任务保持/重启落库验收 | 真实前端上下文选中 marker `ADP_E2E_20260615195204_WOMSTART_HOLD_RESTART` 后执行 start/hold/restart；三次 `updateTaskState` 均返回 `dealSuccessFlag=true`，PostgreSQL 回查任务状态 `runing -> iskeep -> runing`、版本 `1 -> 2 -> 3`，待办状态同步 `runing -> iskeep -> runing` | `/tmp/adp-wom-hold-restart-persistence-current.json`；可复验命令：`ADP_DB_SSH_PASSWORD=... make acceptance-wom-hold-restart-persistence` |
| WOM 制造任务结束最小路径落库验收 | 真实前端上下文选中 marker `ADP_E2E_20260615203324_WOMSTART_STOP` 后执行 start/stop；`findProcReportIdByTaskId`、`outPutCommonTaskEdit`、`layoutJson` 和 `addOutputByOutPutDetails` 均通过；PostgreSQL 回查任务和待办状态为 `WOM_runState/finished` | `/tmp/adp-wom-stop-persistence-current.json`；可复验命令：`ADP_DB_SSH_PASSWORD=... make acceptance-wom-stop-persistence` |
| WOM 完工报工产出明细落库验收 | 真实前端上下文选中 marker `ADP_E2E_20260616001326_WOMSTART_STOP_OUTPUT` 后执行 start，再打开真实 `outPutCommonTaskEdit` 完工报工弹窗提交产出明细；`addOutputByOutPutDetails` 返回 `dealSuccessFlag=true`，PostgreSQL 回查任务/待办/过程报工/执行日志完成，`wom_output_details.output_num=5`，且生成 1 条 `wom_mat_outpt_records` | `/tmp/adp-wom-stop-output-persistence-current.json`；可复验命令：`ADP_DB_SSH_PASSWORD=... make acceptance-wom-stop-output-persistence` |
| WOM 提前放料确认落库验收 | 真实前端上下文选中 marker `ADP_E2E_20260615232454_WOMADVANCE_RELEASE` 后点击“提前放料”并确认；`setAdvanceTrue` 返回 `code=200`、`data.success=true`；PostgreSQL 回查 `advance_charge=true`、`is_advanced=true`、`feed_condition` marker 保留 | `/tmp/adp-wom-advance-release-persistence-current.json`；可复验命令：`ADP_DB_SSH_PASSWORD=... make acceptance-wom-advance-release-persistence` |
| WOM 工序开始后结束落库验收 | 真实前端上下文进入 `makeTaskBatchView`，选中 marker 工序并执行“开始工序 -> 结束工序”；`startProcess/endProcess` 均返回 200；PostgreSQL 回查 `wom_task_processes` 完成、`wom_proc_reports` 新增完成、`wom_process_exelogs` 完成、`wom_wait_put_records` 写入过程、结束时间和工作单元信息 | `/tmp/adp-wom-process-end-persistence-current.json`；可复验命令：`ADP_DB_SSH_PASSWORD=... make acceptance-wom-process-end-persistence WOM_PROCESS_END_PERSISTENCE_OUTPUT=/tmp/adp-wom-process-end-persistence-current.json` |
| WOM 简易活动报工落库验收 | 真实前端上下文进入 `easyTaskOperateView`，选中 marker 活动并点击“结束报工”；`endEasyActive` 返回 `dealSuccessFlag=true`；PostgreSQL 回查活动完成、过程报工完成、待投记录完成、产出明细和物料产出记录 | `/tmp/adp-wom-easy-active-persistence-current.json`；可复验命令：`ADP_DB_SSH_PASSWORD=... make acceptance-wom-easy-active-persistence WOM_EASY_ACTIVE_PERSISTENCE_OUTPUT=/tmp/adp-wom-easy-active-persistence-current.json` |
| WOM 投入明细报工落库验收 | 真实前端上下文进入 `makeTaskBatchView`，选中 marker 投料活动，执行 `startActive -> remainMaterialView/save -> endActive`；投入明细 save 返回 `dealSuccessFlag=true`；PostgreSQL 回查活动完成、过程报工有效、待投记录完成、`wom_putin_details` 写入 marker 批次和数量 | `/tmp/adp-wom-putin-active-persistence-current.json`；可复验命令：`ADP_DB_SSH_PASSWORD=... make acceptance-wom-putin-active-persistence WOM_PUTIN_ACTIVE_PERSISTENCE_OUTPUT=/tmp/adp-wom-putin-active-persistence-current.json` |
| WOM 检查活动报工生成检查结果 | 真实前端上下文进入 `makeTaskBatchView`，选中 `RM_activeType/check` marker 活动并触发 `endActive`；接口返回 `HTTP 200/data.success=true`；PostgreSQL 回查 `wom_pro_check_details` 检查清单和 `wom_check_records` 检查结果均写入 marker 检查项、报出值和生产批号 | `/tmp/adp-wom-check-active-persistence-make.json`；可复验命令：`ADP_DB_SSH_PASSWORD=... make acceptance-wom-check-active-persistence WOM_CHECK_ACTIVE_PERSISTENCE_OUTPUT=/tmp/adp-wom-check-active-persistence-make.json`；marker `ADP_E2E_20260619034356_WOM_ACTIVE`；修复 SQL：`147-wom-pro-check-details-lob-oid-compat.sql` |
| QCS 请检明细 PostgreSQL 缺表修复 | `107-qcs-inspect-detail-tables.sql` 已在测试机执行；回读确认 `qcs_inspect_stds`、`qcs_inspect_coms`、`qcs_report_coms`、`qcs_inspects_di`、`qcs_inspect_reports_di`、`qcs_inspect_releases_di`、`qcs_un_qlf_deals_di` 存在 | SQL：`deploy/docker/postgres/init/107-qcs-inspect-detail-tables.sql`；远端查询：`information_schema.columns` 列数 60/60/70/16/16/16/16 |
| WOM 生产请检生成 QCS 请检单 | 真实前端登录后打开 WOM 制造任务列表，选中 marker `ADP_E2E_20260616050127_WOM_MANU_INSPECT_TASK_TN` 并触发“请检”；`POST /msService/WOM/produceTask/produceTask/createManuInspect` 返回 200/`data.success=true`；随后打开 QCS 制造检验列表确认 marker 批次可见；PostgreSQL 回查任务、待投、执行日志变为 `待检`，`qcs_inspects.id=755756625425664`，`qcs_inspect_stds` 生成 1 条 | `/tmp/adp-wom-manu-inspect-persistence-acceptance-rerun.json`；早期通过报告：`/tmp/adp-wom-manu-inspect-persistence-acceptance.json` |
| QCS 请检单提交到生效并生成报告 | 新 marker `ADP_E2E_20260616084434_WOM_MANU_INSPECT` 执行两次 `POST /msService/QCS/inspect/inspect/bulkSubmit`；第一次生成审核待办 `755811812603136`，第二次将 `qcs_inspects.status` 推到 `99` 并生成 `qcs_inspect_reports.id=755811962860800`、`qcs_report_coms` 1 条和报告编辑待办 `755811963663616` | `/tmp/adp-wom-manu-inspect-persistence-acceptance-after-qcs-report-fix.json`；SQL：`107-qcs-inspect-detail-tables.sql`、`113-qcs-manu-inspect-jbpm-definition.sql`、`117-qcs-report-generation-support.sql`、`118-postgres-legacy-text-bytea-operator.sql` |
| QCS 报告编辑页复验 | 生成的报告编辑 URL 中 `layoutJson`、`editStates` 和 `data/755811962860800` 均为 `200/操作成功`；`222.88.185.146:18080` 真实浏览器渲染 PASS，12 个输入控件和“保存”按钮可见，无 console/page/network/request error | 远端 DB 诊断：`runtime_extra_view.view_json` 已非空，约 79KB，包含 `layoutDatagrid` 和目标 datagrid；证据：`/tmp/adp-qcs-report-edit-render-public-20260618031023/qcs-report-edit-render.json` |
| QCS 产品紧急放行编辑/查看页复验 | `124-qcs-inspect-release-runtime-json.sql` 恢复 edit/view `runtime_extra_view.view_json`；`125-qcs-inspect-release-action-compat.sql` 修复 `qcs_inspect_releases_sv.main_obj` 后，提交后的 view 页 `editStates/data` 均为 `200/操作成功` | 证据：`/tmp/adp-qcs-inspect-release-view-after-sv-fix-20260618/evidence.json`；旧渲染证据：`/tmp/adp-qcs-inspect-release-edit-after-fix-20260618.json`、`/tmp/adp-qcs-inspect-release-list-actions-20260618.json` |
| QCS 产品紧急放行保存/提交落库验收 | 保存草稿 marker `ADP_E2E_20260618_INSP_RELEASE_SAVE_797449` 写入 `qcs_inspect_releases/ec_table_info/wfm_task_pending`；提交审核 marker `ADP_E2E_20260618_INSP_RELEASE_SUBMIT_797449` 生成审核待办 `TaskEvent_0fkkhcq` 和 `wf_deal_info.outcome_des=审核` | 证据：`/tmp/adp-qcs-insp-release-save-ADP_E2E_20260618_INSP_RELEASE_SAVE_797449/evidence.json`、`/tmp/adp-qcs-insp-release-submit-ADP_E2E_20260618_INSP_RELEASE_SUBMIT_797449/submit-evidence.json`；远端 17:01 复查仍存在单据、审核待办和 deal 记录 |
| QCS 报告生效并回写 WOM 合格状态 | Marker `ADP_E2E_20260618044947_QCS_REPORT_EFFECTIVE_RETRY6` 通过 `POST /msService/QCS/inspectReport/inspectReport/batchDealReports` 提交报告待办；LIMS Redis 推送成功，WOM 回调返回 `code=200/message=处理成功`；PostgreSQL 回查报告 `status=99`、待办清零、`wf_deal_info=生效`、WOM 任务 `已检/合格`、批次状态 `11000/qualified` | `/tmp/adp-qcs-report-effective-submit-ADP_E2E_20260618044947_QCS_REPORT_EFFECTIVE_RETRY6.json`；远端 LIMS/WOM 日志；SQL：`120-baseset-act-bat-state-seed.sql` 和 LIMS QCS runtime patch |
| QCS 报告生效并回写 WOM 不合格状态 | Marker `ADP_E2E_20260618_UNQUAL_WOM_BACKFILL_2` 先由真实浏览器 WOM 请检生成 QCS 检验和报告，再通过报告待办两段 `batchDealReports` 提交“不合格”到生效；PostgreSQL 回查报告 `status=99/check_result=不合格`、待办清零、`wf_deal_info=生效`、WOM 任务/待入库/执行日志 `已检/不合格`、批次状态 `11003/unqualified/is_available=false` | `/tmp/adp-qcs-unqualified-wom-backfill-20260618-public-2/wom-manu-inspect-persistence-results.json`；`/tmp/adp-qcs-unqualified-report-effective-submit-2-ADP_E2E_20260618_UNQUAL_WOM_BACKFILL_2.json`；`/tmp/adp-qcs-unqualified-wom-backfill-final-sql-ADP_E2E_20260618_UNQUAL_WOM_BACKFILL_2.txt` |
| QCS `syncInspectReport` 新地址直接回填复验 | Marker `ADP_E2E_20260619_QCSM2` 和 `ADP_E2E_20260619_QCSBAD`；真实前端先执行 WOM `createManuInspect`，随后认证调用 `/msService/WOM/quality/quality/syncInspectReport` 分别传 `reportResult=true/false`；合格分支返回 `needRejectReport=0`，不合格分支返回 `needRejectReport=1`；PostgreSQL 回查 WOM 任务、执行记录、待办和批次字段均按合格/不合格变化 | `/tmp/adp-wom-manu-inspect-persistence-20260619040153/wom-manu-inspect-persistence-results.json`；`/tmp/adp-wom-manu-inspect-persistence-20260619040510/wom-manu-inspect-persistence-results.json`；DB/日志主机 `100.99.133.43` |
| QCS 不合格处理单自动生成 | Marker `ADP_E2E_20260618_UNQLF_AUTO_DEAL_3` 由真实浏览器 WOM 请检生成 QCS 检验和报告，两段 `bulkSubmit` 和两段 `batchDealReports` 提交“不合格”到生效；LIMS 日志确认 `judgeResultUnqualified:true`、`autoUnQlfDeal:manu`、`创建不合格品处理单`；PostgreSQL 回查 `qcs_un_qlf_deals.id=756484120581376/status=88/table_no=manuUnQlfDeal_20260618_001/report_id=756483934967040`，并生成 `TaskEvent_18cp6xa` 待办 | `/tmp/adp-qcs-unqlf-auto-deal-20260618-3/wom-manu-inspect-persistence-results.json`；`/tmp/adp-qcs-unqlf-auto-deal-workflow-ADP_E2E_20260618_UNQLF_AUTO_DEAL_3.json`；`/tmp/adp-qcs-unqlf-auto-deal-final-sql-ADP_E2E_20260618_UNQLF_AUTO_DEAL_3.txt` |
| QCS 不合格处理单审核生效 | Marker `ADP_E2E_20260618_UNQLF_DEAL_EFFECT`；真实页面进入 `manuUnQlfDealView`，带 `workFlowVar.outcome=SequenceFlow_1rfxf3h` 提交“生效”返回 `200/操作成功`；PostgreSQL 回查 `qcs_un_qlf_deals.status=99`、待办清零、DI 写入 `生效`、WOM 任务/执行日志/批次均回填 `rejects_deal_id=756484120581376` | `/tmp/adp-qcs-unqlf-deal-submit-20260618/submit-effective-response-workflowvar-public.json`；`view-before-effective-submit-workflowvar-public.png`；`view-after-effective-submit-workflowvar-public.png` |
| WorkAppointment 作业计划调整/作废/审批 | 列表、编辑、审批页真实前端均可达；`upload-list` edit/approve 修复后返回 200；调整、作废、四段审批到生效均完成 | `/tmp/adp-waps-workplan-persistence-20260619161920/waps-workplan-persistence-results.json`；`/tmp/adp-waps-workplan-persistence-20260619162733/waps-workplan-persistence-results.json` |
| WorkAppointment 编辑页依赖和新增入口复验 | 为恢复出的 WorkAppointment edit/view 静态 HTML 在 `edit.js` 前补齐 `vendors.sesgis.js` 后，真实浏览器打开 `workPlanEdit` 渲染 9 个字段、`workActionEdit` 渲染 22 个字段，console/page/request/network error 均为 0；`148-runtime-visible-business-buttons.sql` 和 `149-workappointment-i18n-resource-fixups.sql` 追加修复后，列表页显示中文 `新增/删除`，点击 `新增` 成功打开 `workPlanEdit` 弹窗 | `/tmp/adp-public-workappointment-after-sesgis-20260618200950/production-action-discovery.json`；`/tmp/adp-workappointment-i18n-fixed-20260619054614/result.json`；`/tmp/adp-workappointment-visible-click-cn-20260619054654/result.json` |
| WOM 生产动作源地图 | 状态流转 `start/hold/restart`、无明细 `stop`、完工报工产出明细、投入明细报工、提前放料确认、下推备料需求、工序开始/结束、活动开始/结束、简易活动报工、制造请检、QCS 报告生成、合格报告生效回写、不合格报告生效回写、不合格处理单自动生成、处理单生效回写、让步放行、拒收 return 和产品紧急放行保存/提交已 PASS；不良数、库存和工作单元设置等剩余写动作仍需 marker 前置数据 | `metadata/production-module-source-action-map.json` |

生产相关已打开页面包括：

| 模块 | 页面/路由 | 当前状态 | 注意 |
| --- | --- | --- | --- |
| WOM | `/msService/WOM/produceTask/produceTask/makeTaskList` | 页面、动作按钮、开始/保持/重启/无明细结束/产出明细报工/投入明细报工/提前放料/生产请检落库 PASS | 制造任务列表可打开，开始/保持/重启/结束/提前放料/请检/生产过程追溯/生成二维码按钮可见；本轮通过 marker 任务验证 start/hold/restart/stop 最小路径、产出明细报工、投入明细报工、提前放料确认和制造请检 |
| WOM | `/msService/WOM/produceTask/produceTask/prepareMakeTaskList` | 页面和动作按钮 PASS | 备料候选列表可打开，下推备料需求按钮可见；当前列表 `共 0 项` |
| WOM | `/msService/WOM/produceTask/produceTask/makeTaskView` | 页面外层 200 / `layoutJson` 200 / 浏览器渲染 PASS | 工序开始/结束、活动开始/结束、产出明细报工、投入明细报工和简易活动报工已通过专项验收；不合格处理和不良数路径仍待专项数据 |
| WOM | `/msService/WOM/produceTask/produceTask/makeTaskBatchView` | 页面外层 200 / `layoutJson` 200 / 浏览器渲染 PASS | 工序开始/结束、活动开始/结束和投入明细报工已完成真实前端和 PostgreSQL 落库验收；`wom_mat_consum_recods=0` 已解释为当前人工投料路径不自动生成活动执行日志/消耗记录，不作为 PostgreSQL 兼容缺口 |
| WOM | `/msService/WOM/produceTask/produceTask/easyTaskOperateView` | 页面外层 200 / `layoutJson` 200 / 浏览器渲染 PASS | 简易活动报工已完成真实前端和 PostgreSQL 落库验收；不合格处理、不良数和库存回写仍需专项验收 |
| WOM | `/msService/WOM/batchMaterial/batMaterilPart/baRetireMentPDAList` | 页面 smoke PASS | 只证明退料 PDA 入口可打开 |
| RM | `/msService/RM/formula/formula/batchFormulaList` | 页面 smoke PASS；`batch/sync` 同步新增 PASS；`batch/delete` 删除软失效 PASS；Excel 导入 PASS；`batchFormulaEdit/View` 页面渲染 PASS | 批控配方入口可打开；marker 配方已通过真实前端上下文同步新增和删除清理落库验收；导入模板/Excel 上传入口已恢复并通过 marker `ADP_E2E_20260619085807_RM_UI_IMPORT` 落库验收；`160-rm-batch-formula-edit-view-runtime-json.sql` 已恢复 edit/view 运行时布局并通过真实浏览器打开；源码、产品手册、远端 `runtime_button/runtime_extra_view/rbac_menuoperate` 和 `100.99.133.43` 真实浏览器按钮探针均确认列表只暴露“下载模板/导入Excel”，编辑入口依赖外部 Batch 客户端 ActiveX/WebSocket 推送 |
| craftGraph | `/msService/craftGraph/basicInfo/basicInfo/basicInfoList` | 页面 smoke PASS；中文 `新增/修改/删除` 可见；点击 `新增` 打开 iframe 编辑弹窗 PASS；物料参照纯 UI 选择、可见保存、可见修改保存和删除点击均已落库 PASS | marker `ADP_E2E_20260619064104_CRAFT_PUREUI` 通过物料参照页选择产品、页面保存/删除和 PostgreSQL 查库；marker `ADP_E2E_20260619071722_CRAFTGRAPH` 通过列表可见 `修改` 保存 V2 并查库 `craft_basic_infos.version=1`；编辑弹窗右侧仍有裁切显示问题，功能不阻断；`craft_tree_structures` 在主表软删后仍 `valid=true`，需业务确认 |
| craftGraph | `/msService/craftGraph/operationButton/buttonConfig/buttonConfList` | 页面 smoke PASS | 只证明工艺按钮配置入口可打开 |
| WTS | `/msService/WTS/workPermit/workPermit/workPermitList` | 页面 smoke PASS；中文 `新增/修改` 可见；点击 `新增` 打开编辑页 PASS；动火正常关闭完整工作流 PASS；`WTS_processing/stop` 终止分支 PASS；独立作废/cancel 入口待产品确认 | 主许可草稿保存/删除、动火子票提交、开票/气体/风险/审批/执行处理/封票生效均已落库 PASS；最终正常关闭 marker `ADP_E2E_20260619142344_WTS_FIREWORK`；终止 marker `ADP_E2E_20260619144549_WTS_FIREWORK_STOP` |
| workAppointment | `/msService/workAppointment/workPlan/workTicketPlan/workPlanList`、`workPlanEdit`、`workActionEdit` | 列表页面 smoke PASS；中文 `新增/删除` 可见；点击 `新增` 打开编辑页 PASS；真实可见表单保存/删除清理 PASS；调整/作废/审批 PASS | `vendors.sesgis.js` 依赖已补齐，编辑页不再空白；`WAPSCommon` 已补齐初始化 `datagridCode`，可见保存 marker `ADP_E2E_20260619091757_WAPS_VISIBLE_FORM_SAVE` 落库 PASS；调整、作废和审批已专项 PASS |
| QCS | `/msService/QCS/inspect/inspect/manuInspectList`、`/msService/QCS/unQlfDeal/unQlfDeal/manuUnQlfDealView`、`/msService/QCS/inspectRelease/inspectRelease/manuInspReleaseList/Edit/View` | 页面 smoke PASS；制造请检落库 PASS；QCS 明细表结构缺口已补；报告生成落库 PASS；报告编辑页 API/render PASS；报告结果保存 PASS；报告生效/WOM 合格回写 PASS；报告生效/WOM 不合格回写 PASS；不合格处理单自动生成 PASS；不合格处理单生效 PASS；让步放行 PASS；拒收 return PASS；产品紧急放行 edit/view/保存/提交 PASS | 已证明制造检验入口可打开，并由 WOM `createManuInspect` 真实前端动作生成 marker QCS 请检单；`bulkSubmit` 到生效后报告和报告明细已落库，报告编辑页 `layoutJson` 已恢复 200 并完成真实渲染；报告结果字段保存已写入 `qcs_inspect_reports`，`batchDealReports` 生效后已分别回写 WOM 合格和不合格状态，并在 `autoUnQlfDeal=manu` 下自动生成不合格处理单和待办；`manuUnQlfDealView/submit` 生效后已回填 WOM `rejects_deal_id`；让步放行由 `PROD-041` 通过，拒收 `return` 由 `PROD-042` 通过；`inspectRelease` 保存草稿已写入 `qcs_inspect_releases/ec_table_info/wfm_task_pending`，提交审核已生成 `TaskEvent_0fkkhcq` 待办和 `wf_deal_info` 审核记录；下一步验收不良数和库存/入库回写 |

## 动作级用例矩阵

| ID | 领域 | 用例 | 前端入口 | 是否落库 | 当前状态 | 阻断/下一步 |
| --- | --- | --- | --- | --- | --- | --- |
| PROD-001 | 工单/任务 | 制造任务列表打开和基础查询 | WOM 制造任务列表 | 否 | PASS | 已捕获列表查询接口 `POST /msService/WOM/produceTask/produceTask/makeTaskList-pending`；读模型识别到 `WOM_PRODUCE_TASKS` 和 `wfm_task_pending` |
| PROD-002 | 工单/任务 | 新建生产工单或制造任务 | WOM 制造任务列表、日计划生成接口 | 是 | PASS | 可见列表页仍没有“新增/新建/添加/创建”按钮，真实浏览器只捕获 `POST /makeTaskList-pending` 查询；该 UI 入口缺口继续记录为产品体验风险。但候选日计划生成路径已完成落库验收：应用 `161`、`162`、`163`、`164` 后，marker `ADP_E2E_20260619195003_PRODUCE_CREATED2_FIX6` 调用 `POST /msService/WOM/produceTask/produceTask/produceTaskCreated2` 返回 `HTTP 200/code=200/message=生成指令单成功`；PostgreSQL 回查 `wom_produce_tasks.id=757036685264128/table_no=produceTask_20260620_001/status=88/valid=true`，并生成 `wom_produce_tasks_di`、活动、质量、批次、批次处理记录、待办、JBPM execution 和流权限。公网真实浏览器打开生成的 `makeTaskEdit` 待办页 `navigationStatus=200`，DOM 值显示生产批号 marker。证据：`/tmp/adp-produceTaskCreated2-persistence-ADP_E2E_20260619195003_PRODUCE_CREATED2_FIX6.json`、`/tmp/adp-produceTaskCreated2-ui-ADP_E2E_20260619195003_PRODUCE_CREATED2_FIX6-dom-values.json` |
| PROD-003 | 状态流转 | 工单开始执行 | WOM 制造任务列表 | 是 | PASS | 真实前端选中 marker `ADP_E2E_20260615192427_WOMSTART` 点击“开始”；`POST /msService/WOM/produceTask/produceTask/updateTaskState` 返回 200/`dealSuccessFlag=true`；回查 `wom_produce_tasks.task_run_state=runing`、`wom_wait_put_records.exe_state=runing`、`wom_proc_reports` 和 `wom_produce_task_exelog` 均有记录 |
| PROD-024 | 状态流转 | 工单保持、重启 | WOM 制造任务列表 | 是 | PASS | Marker `ADP_E2E_20260615195204_WOMSTART_HOLD_RESTART`；真实前端上下文触发 `state=start/hold/restart` 三次请求并回查 `wom_produce_tasks`、`wom_wait_put_records`、`wom_proc_reports`、`wom_produce_task_exelog`。普通 DOM click 在 hold/restart 上被运行时包装层拦截，验收脚本通过浏览器内 React runtime button event 触发真实前端事件，仍需后续单独修按钮可点击性体验 |
| PROD-025 | 状态流转/报工 | 工单结束（无产出明细最小路径） | WOM 制造任务列表、`outPutCommonTaskEdit` | 是 | PASS | Marker `ADP_E2E_20260615203324_WOMSTART_STOP`；真实前端打开结束弹窗并提交 `addOutputByOutPutDetails`，回查 `wom_produce_tasks`、`wom_wait_put_records`、`wom_proc_reports`、`wom_produce_task_exelog` 已变为完成；`outputDetailCount=0`、`taskActItemsCount=0`，只代表无明细结束路径，产出明细另见 `PROD-026`，投入明细另见 `PROD-028` |
| PROD-027 | 状态流转/备料 | 提前放料确认 | WOM 制造任务列表 | 是 | PASS | Marker `ADP_E2E_20260615232454_WOMADVANCE_RELEASE`；真实前端选中运行中且 `advance_charge=true` 的 marker 指令单，点击“提前放料”并确认；`GET /msService/WOM/produceTask/produceTask/setAdvanceTrue/8088156589437949` 返回 `code=200`、`data.success=true`；回查 `wom_produce_tasks.is_advanced=true`、`advance_charge=true`、`feed_condition` 保留 marker，待投记录仍关联原任务 |
| PROD-026 | 报工 | 完工报工产出明细验收 | WOM 制造任务列表、`outPutCommonTaskEdit` | 是 | PASS | Marker `ADP_E2E_20260616001326_WOMSTART_STOP_OUTPUT`；真实前端打开完工报工弹窗并提交产出明细，`addOutputByOutPutDetails` 返回 200/`dealSuccessFlag=true`；PostgreSQL 回查 `wom_produce_tasks.finish_num=5`、`wom_produce_task_exelog.finish_num=5`、`wom_output_details.output_num=5/report_num=5`，且 `wom_mat_outpt_records` 生成 1 条；投入明细另见 `PROD-028` |
| PROD-028 | 报工 | 完工报工投入明细验收 | WOM `makeTaskBatchView`、`remainMaterialView/save` | 是 | PASS | Marker `ADP_E2E_20260616021612_WOM_ACTIVE`；真实前端进入批量页并执行 `startActive -> remainMaterialView/save -> endActive`；投入明细 save 返回 200/`dealSuccessFlag=true`/`id=755716019803392`；PostgreSQL 回查 `wom_putin_details.material_batch_num=ADP_E2E_20260616021612_WOM_ACTIVE_BATCH`、`putin_num/use_num=2`，活动和待投记录已完成；当前人工投料路径不自动生成 `wom_mat_consum_recods` 已由专项源码和远端 SQL 解释 |
| PROD-030 | 状态流转/报工 | 工序开始后结束 | WOM `makeTaskBatchView` | 是 | PASS | Marker `ADP_E2E_20260616015500_WOM_ACTIVE`；真实前端打开批量页，选中 marker 工序并执行 `startProcess/endProcess`；PostgreSQL 回查 `wom_task_processes.process_run_state=WOM_runState/finished`、工序报工 `wom_proc_reports` 完成、`wom_process_exelogs` 完成、`wom_wait_put_records` 写入过程 `actual_start_time/actual_end_time/proc_report_id` 和工作单元信息；`106-wom-process-wait-end-time-sync.sql` 已纳入 PostgreSQL 兼容补丁 |
| PROD-029 | 报工 | 简易活动报工结束 | WOM `easyTaskOperateView` | 是 | PASS | Marker `ADP_E2E_20260616004615_WOM_ACTIVE`；真实前端打开简易活动页，选中 marker 活动并点击“结束报工”；`POST /endEasyActive/8388157077549689` 返回 200/`dealSuccessFlag=true`/`exeState.id=WOM_runState/finished`；PostgreSQL 回查 `wom_task_actives.is_finish=true`、`wom_proc_reports.is_finish=true`、`wom_wait_put_records.exe_state=finished`、`wom_output_details.output_num/report_num=3`，且 `wom_mat_outpt_records` 生成 1 条；`wom_acti_exelogs.act_end_time` 仍为空，需后续解释或修复 |
| PROD-004 | 指令 | 制造指令单生成或维护 | `makeTaskEdit`、WOM 制造任务/备料制造指令列表 | 是 | PASS | `makeTaskEdit` 页面渲染已通过，且日计划生成制造指令单已真实落库：marker `ADP_E2E_20260619195003_PRODUCE_CREATED2_FIX6` 调用 `produceTaskCreated2` 后生成 `wom_produce_tasks.id=757036685264128/table_no=produceTask_20260620_001`、编辑待办 `757036687533312` 和 `makeTaskFlow-1`。公网真实浏览器打开待办编辑页 `navigationStatus=200/title=制造指令单`，无 console/page/request failure，表单值包含生产批号 marker。`/msService/public/.../produceTaskCreated` 已显式禁用：安全探针返回 `HTTP 200/code=400` 且查库为 0，不能作为创建证据。列表/菜单仍缺可见“新增/保存/导入”按钮，后续要恢复用户可见入口或导入模板，但不再阻断 `produceTaskCreated2` 生成路径验收 |
| PROD-005 | 派工 | 工序/人员/班组派工 | `processUnitEdit`、TeamInfo 班组/排班相关入口 | 是 | PASS | 2026-06-20 SQL `159-wom-process-unit-edit-runtime-json.sql` 恢复 `WOM_1.0.0_produceTask_processUnitEdit` 的 `runtime_extra_view.view_json` 和 `runtime_view.extra_view/ec_env` 链接，并重启 `baseService` 清缓存；公网真实浏览器打开 `processUnitEdit?id=8388189129735082`，`layoutJson/editStates/upload-list` 均 200 且无 console/page/request failure。提交 `POST /msService/WOM/produceTask/taskProcess/processUnitEdit/submit?id=8388189129735082` 返回 200/`dealSuccessFlag=true`；PostgreSQL 回查 marker `ADP_E2E_20260619174817_WOM_ACTIVE_WU` 已写入 `wom_task_processes.equipment_id` 并同步 `wom_wait_put_records` 的 process/workOrder 行 `euq_id/equ_code/equ_name`。TeamInfo 排班计划主表新增仍由 `ADP_E2E_20260619030900_TEAMINFO_SCHEDULE_PLAN` 单独 PASS；未发现独立 WOM 人员/班组派工单，后续若业务包新增入口再补单独用例 |
| PROD-006 | 退料 | 退料 PDA 页面打开 | WOM 退料 PDA 列表 | 否 | PASS | 已有页面/API smoke 证据 |
| PROD-007 | 退料 | 退料申请提交、审核或回写库存 | WOM 退料/拒收列表 | 是 | PASS | 列表/查询 smoke 已 PASS；后续专项已完成三条退料主链路真实前端/API/PostgreSQL 验收。备料退料 marker `ADP_E2E_20260618175855_WOM_REJECT_MATERIAL` 保存并携带 `workFlowVar.outcomeMap` 提交生效，`wom_reject_materials.id=756655464850688` 变为 `status=99/effect_time` 且待办清零；车间物料退料 marker `ADP_E2E_20260618185904_WOM_REJECT_MATERIAL` 经发起、接收、生效后，`wom_rejct_matal_parts.reject_num=2.000000`，`hm_factory_materias.availi_quantity` 从 `10.00` 扣减到 `8.00`；批次退料 marker `ADP_E2E_20260618192824_WOM_REJECT_MATERIAL` 生效后，`wom_batch_mat_needs/wom_batch_materils/wom_bat_materil_parts.return_num` 均从 `0` 回写到 `2`，配料明细 `retirement_state=WOM_retirementState/returned`。最终仓储/库存服务总账仍归 `PROD-022` material 服务缺失跟踪 |
| PROD-008 | 配方 | 批量配方列表打开 | RM 批量配方列表 | 否 | PASS | 已有页面/API smoke 证据 |
| PROD-009 | 配方 | 批量配方导入模板下载和 Excel 导入 | RM 批量配方列表 | 是 | PASS | 已恢复 `batchFormulaList` 运行时页面为 `LIST/list.js` 并补回 `downloadXls/importMainXls` 入口；真实浏览器在同环境公网入口 `http://222.88.185.146:18080` 可见“下载模板/导入Excel”，点击导入触发 file chooser 并上传 `/tmp/adp-rm-ui-import-20260619085807/rm-import-ui.xls`。`POST /inter-api/file-server/v1/file/upload/file` 返回 `temp/1781834329399/rm-import-ui.xls`，`GET /msService/RM/formula/formula/importMainXls?...` 返回 `success|导入成功！共导入1行记录！`；`100.99.133.43` PostgreSQL 回查 `rm_formulas.id=756773410223360/table_no=formula_20260619_005/formual_code=ADP_E2E_20260619085807_RM_UI_IMPORT_FCODE/formula_name=ADP_E2E_20260619085807_RM_UI_IMPORT_FNAME/status=99`。证据 `/tmp/adp-rm-ui-browser-import-public-staticpatched-20260619095601/result.json`；Tailscale 入口仍有大静态包慢链路风险，DB/SSH 证据走 `100.99.133.43` |
| PROD-010 | 配方 | 批量配方可见显式编辑入口 | RM 批量配方列表 | 是 | BLOCKED | `160-rm-batch-formula-edit-view-runtime-json.sql` 已恢复 `batchFormulaEdit/View` 的 `layoutJson`；公网真实浏览器打开 edit/view 均 `nav=200`、`layoutJson=200/code=200`、无 console/page/request failure，证据 `/tmp/adp-rm-batch-formula-page-smoke-public-current.json`。2026-06-20 在新测试地址 `100.99.133.43` 复核：`runtime_button` 对 `batchFormula%` 为 0 条，`rbac_menuoperate` 只有 query；`runtime_extra_view` 的 `batchFormulaList` 仅有“下载模板/导入Excel”两个按钮；真实浏览器鉴权打开 `batchFormulaList?system=formulaEnableFlw` 为 `navigationStatus=200/layoutJson=200`、无 console/request failure，截图 `/tmp/adp-rm-batchFormulaList-button-probe-1781899978217/batchFormulaList.png` 同样只显示下载/导入。源码 `batchEdit.html` 和产品手册显示手工编辑依赖 IE11 ActiveX、Batch 服务器和 WebSocket 推送打开 `batchFormulaEdit`，当前测试环境没有外部 Batch 客户端联调，不能用平台假按钮替代。`batch/sync` 新增和 `batch/delete` 删除清理已由 `PROD-043` 单独 PASS |
| PROD-043 | 配方 | 批控配方同步新增和删除清理 | RM 批量配方列表 | 是 | PASS | Marker `ADP_E2E_20260619063244_RMBATCH_RECHECK`；真实登录后打开 `http://100.99.133.43:18080/msService/RM/formula/formula/batchFormulaList?system=formulaEnableFlw`，导航 `200`，同一浏览器上下文调用 `POST /msService/RM/formula/formula/batch/sync` 返回 `200/success=true`，PostgreSQL 回查 `rm_formulas.id=756722825766144/batch_formulaid=1781821964465576/formula_name` 含 marker、`valid=true/status=88`；随后调用 `POST /msService/RM/formula/formula/batch/delete?batchFormulaId=1781821964465576` 返回 `200/dealSuccessFlag=true`，回查同一配方 `valid=false/status=0` 且有效 `rm_process_actives=0`。console/page/request failure 均为 0。证据 `/tmp/adp-rm-batch-sync-persistence-20260618223244/rm-batch-sync-persistence-results.json` |
| PROD-011 | 工艺 | 工艺基础信息列表打开 | craftGraph 工艺基础信息 | 否 | PASS | 已有页面/API smoke 证据 |
| PROD-012 | 工艺 | 工艺路线或基础信息新增、编辑、删除 | craftGraph 工艺基础信息 | 是 | PASS | 真实浏览器已完成组合验收。`148-runtime-visible-business-buttons.sql` 恢复 `新增/删除`，`150-craftgraph-visible-modify-button.sql` 恢复 `修改` 并重启 `baseService` 清缓存。marker `ADP_E2E_20260619064104_CRAFT_PUREUI` 通过物料参照弹窗纯 UI 选择产品、点击页面 `保存` 并查库 `craft_basic_infos.valid=true/version=0/product=8991071330917338`，随后可见 `删除/确认` 后主表 `valid=false/version=1`。marker `ADP_E2E_20260619071722_CRAFTGRAPH` 通过列表可见选中、点击 `修改`、iframe `basicInfoEdit?id=756851757360384` 打开、保存 `ADP_E2E_20260619071722_CRAFTGRAPH_UPDATED_NAME/V2/updated ...`，`POST /basicInfoEdit/submit?id=756851757360384` 返回 `operate=edit/dealSuccessFlag=true`，PostgreSQL 回查 `craft_basic_infos.version=1` 且字段更新，`craft_tree_structures.tree_node_id=756851757360384/valid=true/version=1`。证据 `/tmp/adp-craftgraph-persistence-current.json`、`/tmp/adp-craftgraph-pureui-ADP_E2E_20260619064104_CRAFT_PUREUI/result.json`、`/tmp/adp-craftgraph-pureui-delete3-ADP_E2E_20260619064104_CRAFT_PUREUI/result.json`。残余：编辑弹窗右侧字段有裁切；删除后 `craft_tree_structures.valid=true` 是否需同步软删仍待业务确认，但不再阻断新增/编辑/删除主流程验收 |
| PROD-013 | 工艺 | 工艺按钮配置列表打开 | craftGraph 按钮配置 | 否 | PASS | 已有页面/API smoke 证据 |
| PROD-014 | 作业许可 | 作业许可列表打开 | WTS 作业许可列表 | 否 | PASS | 2026-06-20 复测发现 `workPermitList-query` 曾因 `runtime_extra_view.config/full_config` 为空和历史 `wts_work_permits.payload=202062` 数字串触发后端 500；`165-wts-workpermit-list-runtime-compat.sql` 补齐 LISTPT config 并把 payload 规范为 `{"workTickets":{"fireWork":ticketId}}` 后，认证接口返回 `HTTP 200/code=200/message=操作成功/total=8`，6 页生产列表浏览器探针中 WTS 页 `nav=200` 且无 failed response/console/page error |
| PROD-015 | 作业许可 | 作业许可新建、提交、审批、关闭 | WTS 作业许可列表 | 是 | PASS | Marker `ADP_E2E_20260619142344_WTS_FIREWORK`；公网真实浏览器 E2E 登录后创建 WTS 作业许可和动火子票，依次提交 `fireworkEdit/fireworkGas/fireworkRisk/fireworkApproval/fireworkDeal/fireworkClose`。SSH/DB/日志使用 `100.99.133.43`，PostgreSQL 确认 `wts_work_tickets.id=6599234028094016 status=99 job_status=WTS_jobStatus/normalClose process_method=WTS_processing/close check_results_value=ADP_E2E_20260619142344_WTS_FIREWORK_CLOSE_ACCEPT`，活动待办数为 0，`wf_deal_info` 写入到 `TaskEvent_0r1w2fv/生效`。旧阻断已由 `155-postgres-legacy-text-bigint-like-operator.sql`、`WorkTicket2PermitService` fallback、`WorkTicketEvent` 空值保护/结束置状态和 `AutoNodeEvent` 可选事件隔离修复；证据 `/tmp/adp-wts-firework-persistence-20260619142344/wts-firework-persistence-results.json`。补充：`WTS_processing/stop` 终止分支 marker `ADP_E2E_20260619144549_WTS_FIREWORK_STOP` 已 PASS，最终 `status=99/job_status=WTS_jobStatus/errorClose/process_method=WTS_processing/stop/pending=0`；若产品另有独立作废/cancel/delete 入口仍需确认。`100.99.133.43:18080` 页面直连仍有 503/超时慢链路风险，浏览器证据使用同环境公网入口 |
| PROD-016 | 作业计划 | 作业计划列表打开 | workAppointment 作业计划列表 | 否 | PASS | 已有页面/API smoke 证据 |
| PROD-017 | 作业计划 | 作业计划新建、调整、作废、审批生效 | workAppointment 作业计划列表 | 是 | PASS | `workPlanEdit`/`workActionEdit` 已渲染且无前端错误；可见新增、保存、删除清理由 marker `ADP_E2E_20260619091757_WAPS_VISIBLE_FORM_SAVE` PASS。新增专项 marker `ADP_E2E_20260619161920_WAPS_WORKPLAN` 完成新建、调整、作废，PostgreSQL 确认调整后 `version=2/pending_count=1/di_count=2`，作废后 `status=0/pending_count=0/di_count=3`；marker `ADP_E2E_20260619162733_WAPS_WORKPLAN_APPROVE` 完成四段审批到生效，最终 `status=99/pending_count=0/di_count=5`。`156`/`157`/`158` SQL 与 `patch-workappointment-upload-list-runtime-compat.py` 已纳入 PostgreSQL/runtime 兼容。证据 `/tmp/adp-waps-workplan-persistence-20260619161920/waps-workplan-persistence-results.json`、`/tmp/adp-waps-workplan-persistence-20260619162733/waps-workplan-persistence-results.json`。若产品另有独立 cancel/delete/作废入口，需单独确认后作为新用例验收 |
| PROD-018 | 质量联动 | 制造检验列表打开 | QCS 制造检验列表 | 否 | PASS | 已有页面/API smoke 证据 |
| PROD-031 | 质量联动 | 制造请检生成 QCS 请检单 | WOM 制造任务列表 -> QCS 制造检验列表 | 是 | PASS | Marker `ADP_E2E_20260616050127_WOM_MANU_INSPECT`；真实前端触发 `POST /msService/WOM/produceTask/produceTask/createManuInspect` 并打开 QCS 列表复验；PostgreSQL 回查 `wom_produce_tasks/wom_wait_put_records/wom_produce_task_exelog` 进入 `待检`，`qcs_inspects.id=755756625425664`，`qcs_inspect_stds` 生成 1 条 |
| PROD-032 | 质量联动 | QCS 请检单提交到生效并生成报告 | QCS 制造检验列表 | 是 | PASS | Marker `ADP_E2E_20260616084434_WOM_MANU_INSPECT`；两次 `bulkSubmit` 均返回 `200`，`qcs_inspects.status=99/refable=true/check_state=QCS_checkState/reporting`，`qcs_inspect_reports.id=755811962860800`、`qcs_report_coms` 1 条、报告编辑待办 `755811963663616` 均已查库确认；LIMS `syncEntity` NPE 仍作为残余风险记录 |
| PROD-033 | 质量联动 | QCS 检验报告编辑页打开 | QCS 检验报告编辑页 | 否 | PASS | `layoutJson`、`editStates` 和 `data/755811962860800` 已为 `200`，`runtime_extra_view.view_json` 已恢复；`222.88.185.146:18080` 真实浏览器渲染 PASS，保存按钮可见且无 console/page/network/request error。`100.99.133.43:18080` 仍有慢链路风险 |
| PROD-034 | 质量联动 | QCS 检验报告结果保存 | QCS 检验报告编辑页 | 是 | PASS | Marker `ADP_E2E_20260618033224_QCS_RESULT_SAVE`；真实页面运行时执行保存/提交，LIMS 日志显示 `/QCS/inspectReport/inspectReport/manuInspReportEdit/submit` 正常结束；PostgreSQL 确认 `qcs_inspect_reports.id=755811962860800 check_result=LIMSBasic_standardGrade/Qualified memo_field=ADP_E2E_20260618033224_QCS_RESULT_SAVE memo version=3`，`qcs_report_coms` 关联行有效。本项只证明结果字段保存，不代表不合格处理、坏品或生产/库存回写 |
| PROD-035 | 质量联动 | QCS 检验报告生效并回写 WOM 合格状态 | QCS 检验报告编辑页 -> WOM `syncInspectReport` | 是 | PASS | Marker `ADP_E2E_20260618044947_QCS_REPORT_EFFECTIVE_RETRY6`；`POST /msService/QCS/inspectReport/inspectReport/batchDealReports` 返回 HTTP 200；LIMS/WOM 日志确认 Redis 和 WOM 回调成功；PostgreSQL 回查 `qcs_inspect_reports.status=99/version=6`、报告待办清零、`wf_deal_info.outcome_des=生效`、WOM 任务 `check_state=已检/check_result=合格`、批次 `active_batch_state_id=11000/check_result=qualified` |
| PROD-036 | 质量联动 | QCS 检验报告生效并回写 WOM 不合格状态 | QCS 检验报告编辑页 -> WOM `syncInspectReport` | 是 | PASS | Marker `ADP_E2E_20260618_UNQUAL_WOM_BACKFILL_2`；真实浏览器先触发 WOM `createManuInspect`，两次 QCS `bulkSubmit` 生成报告，再两次 `batchDealReports` 将“不合格”报告提交到生效；PostgreSQL 回查 `qcs_inspect_reports.status=99/check_result=不合格`、报告待办清零、`wf_deal_info.outcome_des=生效`、WOM 任务/待入库/执行日志 `已检/不合格`、批次 `active_batch_state_id=11003/check_result=unqualified/is_available=false`；该历史 marker 运行时 `qcs_un_qlf_deals=0`，自动不合格处理单已由 `PROD-037` 使用新 marker 单独验收 PASS |
| PROD-037 | 质量联动 | QCS 不合格报告自动生成不合格处理单 | QCS 检验报告编辑页 -> `manuUnQlfDealWorkFlow` | 是 | PASS | Marker `ADP_E2E_20260618_UNQLF_AUTO_DEAL_3`；真实浏览器先触发 WOM `createManuInspect`，两次 QCS `bulkSubmit` 生成报告，再两次 `batchDealReports` 将“不合格”报告提交到生效；LIMS 日志确认 `judgeResultUnqualified:true`、`autoUnQlfDeal:manu`、`创建不合格品处理单`；PostgreSQL 回查报告 `756483934967040` `status=99/check_result=不合格/un_qlf_deal_flag=true`、`qcs_un_qlf_deals.id=756484120581376` `status=88/table_no=manuUnQlfDeal_20260618_001/report_id=756483934967040`、处理单待办 `756484121699584/TaskEvent_18cp6xa`，WOM 任务和批次均回写为不合格 |
| PROD-038 | 质量联动 | QCS 不合格处理单审核生效并回写 WOM 处理单号 | QCS 不合格处理单查看页 -> WOM `syncRejectReport` | 是 | PASS | Marker `ADP_E2E_20260618_UNQLF_DEAL_EFFECT`；真实页面上下文打开 `manuUnQlfDealView` 并提交 `SequenceFlow_1rfxf3h/生效`。第一次未带 `workFlowVar` 的 submit 返回 200 但查库只保存备注、未生效；第二次带 `deploymentId/pendingId/workFlowVar` 返回 `200/操作成功`。PostgreSQL 回查 `qcs_un_qlf_deals.id=756484120581376 status=99`、待办清零、`qcs_un_qlf_deals_di` 新增 `SequenceFlow_1rfxf3h/生效`，`wom_produce_tasks`、`wom_produce_task_exelog`、`baseset_batch_infos` 均写入 `rejects_deal_id=756484120581376`。WOM NPE 已由 `123-wom-qcs-null-boolean-compat.sql` 和 WOMMs boot jar 空安全 class 修复；LIMS `syncEntity` 仍有被捕获 NPE 残留日志但不影响本次事务提交 |
| PROD-041 | 质量联动 | QCS 不合格处理单让步放行处理方式生效并回写 WOM | QCS 不合格处理单列表/编辑/查看页 -> WOM `syncRejectReport` | 是 | PASS | Marker `ADP_E2E_20260618210018_QCS_DEGRADED_RELEASE`；`139-baseset-qcs-deal-way-state-seed.sql` 补齐 `degradedRelease(11010)` 和 `return(11011)`，真实页面上下文创建处理单、审核并生效。PostgreSQL 回查 `qcs_un_qlf_deals.status=99/act_bat_state_id=11010`，待办清零，WOM 任务/执行日志/批次均回写 `rejects_deal_id=756700072772864`，批次 `deal_type=BaseSet_dealType/degradedRelease` |
| PROD-042 | 质量联动 | QCS 不合格处理单拒收 return 处理方式生效并回写 WOM | QCS 检验报告 -> 不合格处理单编辑/查看页 -> WOM `syncRejectReport` | 是 | PASS | Marker `ADP_E2E_20260619_QCS_RETURN_1`；真实浏览器从 WOM `createManuInspect` 生成 QCS 检验申请，QCS 两段 `bulkSubmit` 和两段 `batchDealReports` 将报告提交为不合格生效并自动生成处理单 `756714615715072`。编辑页选择 `actBatStateId=11011/return` 后审核、生效均返回 `200/操作成功`；第一次用普通日期字符串提交曾返回 500，已保留失败证据并改用前端可解析日期格式重试。PostgreSQL 回查报告 `756714487547136 status=99/check_result=不合格/un_qlf_deal_flag=true`，处理单 `status=99/act_bat_state_id=11011`，待办清零，WOM 任务/执行日志/批次均回写 `rejects_deal_id=756714615715072`，批次 `deal_type=BaseSet_dealType/return`。证据 `/tmp/adp-qcs-return-persistence-ADP_E2E_20260619_QCS_RETURN_1/deal-return-retry-evidence.json` |
| PROD-039 | 质量联动 | QCS 产品紧急放行编辑/查看页打开 | QCS 产品紧急放行列表/编辑/查看 | 否 | PASS | `124-qcs-inspect-release-runtime-json.sql` 恢复 `QCS_5.0.0.0_inspectRelease_manuInspReleaseEdit/View` 的 `runtime_extra_view.view_json`；`125-qcs-inspect-release-action-compat.sql` 修复提交后 view/data 依赖的 `qcs_inspect_releases_sv.main_obj`；公网真实浏览器 view 页 `editStates/data` 返回 200。证据 `/tmp/adp-qcs-inspect-release-view-after-sv-fix-20260618/evidence.json`。页面仍有重复 auth 401 和“请重新登录”覆盖层残留，需单独治理 |
| PROD-040 | 质量联动 | QCS 产品紧急放行保存草稿并提交审核 | QCS 产品紧急放行编辑页 | 是 | PASS | Marker `ADP_E2E_20260618_INSP_RELEASE_SAVE_797449` 保存草稿返回 `200`，PostgreSQL 回查 `qcs_inspect_releases.id=756521397810432/table_no=manuRelease_20260618_001/table_info_id=756521397814528/status=88`，`ec_table_info` 同步写入，编辑待办 `756521399026944/TaskEvent_1g5tdqv` 生成；Marker `ADP_E2E_20260618_INSP_RELEASE_SUBMIT_797449` 提交 `SequenceFlow_0ecewt8/审核` 返回 `200`，远端复查同一单据 `version=3`，审核待办 `756521872426240/TaskEvent_0fkkhcq` 和 `wf_deal_info.outcome_des=审核` 均存在。证据 `/tmp/adp-qcs-insp-release-save-ADP_E2E_20260618_INSP_RELEASE_SAVE_797449/evidence.json`、`/tmp/adp-qcs-insp-release-submit-ADP_E2E_20260618_INSP_RELEASE_SUBMIT_797449/submit-evidence.json` |
| PROD-019 | 质量联动 | QCS 不良数和库存/入库相关回写 | QCS 检验报告编辑页、WOM/库存回写 | 是 | BLOCKED | QCS 明细/DI 表缺口已由 `107-qcs-inspect-detail-tables.sql` 补齐，制造请检、报告生成、报告编辑页、结果保存、合格/不合格报告生效回写、自动不合格处理单、处理单生效回写、紧急放行、让步放行 `degradedRelease` 和拒收 `return` 均已通过；2026-06-19 用 `ADP_E2E_20260619_QCSM2/QCSBAD` 在新地址复验 `syncInspectReport` 合格/不合格直接回填 WOM 状态和批次可用性均 PASS；2026-06-20 审计 `docs/backend-table-audit/wom-bad-quantity-analysis.md` 确认当前恢复包没有独立的不良数/坏品数量前端入口、源码 endpoint 或 PostgreSQL 目标列，现有形态是 `qcs_inspects.quantity` 请检数量 + `check_result` 检验结论 + `qcs_un_qlf_deals` 不合格处理单 + WOM `rejects_deal_id/is_available` 回写。库存/入库仍被缺失的 `material` 服务阻断 |
| PROD-020 | 追溯 | 批次、物料、工单追溯查询 | WOM 制造任务列表“生产过程追溯”按钮 -> `ProcessAnalysis` | 可能 | BLOCKED | 入口函数 `prodprocessView` 已定位：先请求 `/msService/ProcessAnalysis/analysisParam/analysisParam/isProdprocessView?batchNo=...`，再打开 `/msService/ProcessAnalysis/processAnalysis/exelogSecond/processBatchViewOut?...`；公网真实浏览器用 marker 批次 `ADP_E2E_20260618_UNQLF_AUTO_DEAL_3_BATCH` 请求两个 URL 均返回 `503`。2026-06-20 在 `100.99.133.43` 认证复验 5 个 `ProcessAnalysis` 入口均返回 `503 can not find any tenant app service`；Nacos `prod@@ProcessAnalysis/processanalysis/PROCESSANALYSIS/Traceability/traceability` 均为空实例，`WOMMs` 有健康实例；PostgreSQL 严格扫描 `^pa_`、`process.*analysis`、`trace`、`process_batch` 为 0 张表，`runtime_view/rbac_menuinfo` 无对应行。本地源码只发现 WOM 调用方事件脚本，无被调 Java 实现或模块包。需要补 ProcessAnalysis 模块包/服务/视图/表后复验，详见 `docs/backend-table-audit/processanalysis-dependency-analysis.md` |
| PROD-021 | 报工 | 完整工序报工、产量、不良数登记 | WOM `makeTaskView/makeTaskBatchView/easyTaskOperateView` | 是 | BLOCKED | 源码已定位 `startProcess/endProcess`、`addOutputByOutPutDetails/endEasyActive`、`remainMaterialView/save` 和 `WOM_PROC_REPORTS/WOM_PUTIN_DETAILS` 等表；工序 `startProcess/endProcess`、活动 `startActive/endActive`、产出明细、投入明细、检查活动记录和简易活动报工已专项 PASS，投入明细消耗记录未生成已按当前源码路径解释；QCS 合格/不合格结果回填 WOM 已用新 marker 复验 PASS；2026-06-20 审计确认当前恢复包没有独立不良数登记字段或目标表，完整报工剩余 blocker 应表述为“若产品手册要求独立坏品数量，则需新业务包提供字段/路由/表映射；否则当前可验收边界是产量、检验结论、不合格处理和 material 库存回写” |
| PROD-022 | 入库 | 完工入库或库存回写 | WOM/QCS -> material 服务 | 是 | BLOCKED | 源码定位到 `WOMQCSServiceImpl.checkResultToWms` 调 `material /material/foreign/foreign/checkProdResult`，`WOMPutInMaterialServiceImpl` 与 `WOMMatConsumRecodServiceImpl` 调 `material /public/material/produceOutSingle/produceOutSing/generateProduceOutSing`。测试环境 Nacos `prod` 组无 `material` 实例，认证后两个 material 网关接口均返回 `503 can not find any tenant app service`，`BasicMs/BaseSet/LIMSMaterial` 替代路径为 `404`；最新本地包扫描覆盖 `58889` 文件、`1142` 外层 archive、`2139` 个第一方/依赖相关嵌套 archive 和 `429984` 个 archive entry，entry/text 上限跳过均为 `0`，只发现 `LIMSMaterial`、基础物料/仓库档案与 `SpareManage` 备件库存，不包含上述被调接口实现；远端 PostgreSQL 也只有基础物料/仓库和 WOM 内部物料表。2026-06-20 复验证据见 `docs/backend-table-audit/material-service-dependency-analysis.md`；需要补仓储/库存 material 服务包和目标表后复验 |
| PROD-023 | 导出 | 生产相关列表导出 | WOM/RM/WTS/QCS 相关列表 | 否 | BLOCKED | 2026-06-22T01:50:35.404Z 当前地址复测：API base 为 `http://100.99.133.43:18080`，真实浏览器入口为同一测试环境公网地址 `http://222.88.185.146:18080`；本轮 `ADP_PAGE_TIMEOUT_MS=240000` 下 WOM 制造任务、RM 批量配方、WTS 作业许可、QCS 检验报告、不合格处理、产品紧急放行 6 个生产列表均可打开，`pagePass=6`，因此导出阻断不再归因于页面超时。仓库内可复验 smoke `make smoke-production-export-readiness ADP_BASE_URL=http://100.99.133.43:18080 ADP_BROWSER_BASE_URL=http://222.88.185.146:18080 PRODUCTION_EXPORT_SMOKE_OUTPUT=metadata/production-export-readiness-smoke.json ADP_PAGE_TIMEOUT_MS=240000 ADP_API_TIMEOUT_MS=30000` 会执行真实浏览器页面、`layoutJson`、`downloadXls` 文件响应、源码/运行时配置 sourceAudit 和逐目标 `acceptanceContract` 复验，当前 `visibleExportActions=0`、`runtimeExportActions=0`、`nonEmptyDownloads=1`、`backendQueryExportWorkbooks=1`、`backendQueryExportErrors=4`、`verifiedDataExports=0`、`ready=0`、`actionRequired=1`、`blocked=5`，因此仍为 BLOCKED。WTS `workPermitList-query?exportFlag=true` 返回 `200/OLE_XLS/8704`，但真实页面没有可见导出动作、layoutJson 没有 runtime 导出动作、sourceAudit 仍找不到目标导出 hook，所以 WTS 单项只能记为 `ACTION_REQUIRED`；WOM 与 3 个 QCS query-export 仍返回 500/JSON，RM query-export 返回 200/JSON，直接 `downloadXls` 仍为空或导入模板形态。sourceAudit 同时证明平台通用 export 模板存在，但 6 个目标生产页 `targetExportMatches=0`；WOM/RM/WTS/QCS 检验报告分类为 `DOWNLOADXLS_IMPORT_TEMPLATE_OR_EMPTY_ENDPOINT_ONLY`，QCS 不合格处理/紧急放行分类为 `NO_TARGET_EXPORT_SOURCE_FOUND`。证据：`metadata/production-export-readiness-smoke.json` |

## 验收执行规则

动作级用例从 `NOT_RUN` 或 `BLOCKED` 变成 `PASS` 前，必须满足：

1. 使用真实浏览器或等效 E2E 操作。
2. 写操作必须带唯一 marker，例如 `ADP_E2E_YYYYMMDD_HHMMSS_PROD_TASK`。
3. 捕获 method、URL、payload、response status 和 response body。
4. 追踪 Controller/Service/Mapper/DAO 或反编译后端入口。
5. 查询 PostgreSQL 目标表证明新增、编辑、删除、状态流转或文件导入确实落库。
6. 如果发现缺表、缺列、类型不兼容或 Oracle 方言残留，必须补幂等 SQL 或记录到模块 backlog。

## 后续优先级

1. 先执行 QCS 不良数和库存/入库回写专项验收；QCS 合格/不合格回填 WOM 状态已在 `100.99.133.43` 对应测试环境用 `ADP_E2E_20260619_QCSM2/QCSBAD` 复验通过，让步放行和拒收处理方式已专项 PASS；`QCS_5.0.0.0_inspectReport_manuInspReportEdit` 的 `runtime_extra_view.view_json`、`layoutJson`、真实页面渲染、结果字段保存、合格报告生效/WOM 回写、不合格报告生效/WOM 回写、自动处理单生成和处理单生效回写均已通过，`QCS_5.0.0.0_inspectRelease_manuInspReleaseEdit/View` 页面渲染、保存草稿和提交审核也已通过，报告生成落库已经通过 `ADP_E2E_20260616084434_WOM_MANU_INSPECT` 复验，不能再作为当前 blocker。
2. 如果产品确认人工投料结束也必须生成消耗记录，再补服务层活动执行日志/物料消耗链路设计和专项断言，不能用数据库触发器硬补。
3. 同步追 `PROD-002` 的用户可见创建入口；当前 `produceTaskCreated2` 日计划生成路径已落库 PASS，但制造任务列表仍没有显式新增按钮。
4. `PROD-015` 正常关闭、未选处理方式业务校验和 `WTS_processing/stop` 终止分支已 PASS；后续只在产品确认存在独立作废/cancel/delete 入口时补测该入口。`PROD-019` 不良数/库存回写；`PROD-007` 退料主链路已 PASS，最终库存总账并入 `PROD-022` material 服务缺失；`PROD-010` 的 edit/view 页面已恢复，剩余验收需要外部 Batch 客户端/IE ActiveX 触发 WebSocket 推送编辑闭环；`PROD-023` 需产品确认是否应补导出按钮/接口。
5. 最后补追溯和完工入库：追溯当前缺 `ProcessAnalysis` 模块，本地目录、源码调用方扫描和远端 Nacos/DB 均未找到可运行模块；完工入库/库存回写当前缺 `material` 租户服务，当前包内只找到基础物料、LIMS 物料和备件库存，不包含 WOM/QCS 调用的 `material/foreign`、`produceOutSingle` 服务端接口，Nacos `prod` 组无实例且认证后网关返回 `503 can not find any tenant app service`，需要补仓储/库存模块包、服务注册和表级地图。详见 `docs/backend-table-audit/processanalysis-dependency-analysis.md` 和 `docs/backend-table-audit/material-service-dependency-analysis.md`。
